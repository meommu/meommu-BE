spring:
  datasource.url: jdbc:h2:mem:testdb;NON_KEYWORDS=USER;DATABASE_TO_UPPER=false
  sql.init.mode: never

security:
  jwt:
    token:
      secret-key: test.test.test
      expire-length:
        access: 3600000